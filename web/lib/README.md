# web/lib

Service interfaces + impls, i18n config, and generated TS models live here.
Populated per module as screens are ported off `SendRate-Web/index.html`.

- `services/` — rate, alerts, persistence (IndexedDB via Dexie). See docs/services/.
- `i18n/` — i18next (en/es/tl). See docs/architecture/localization.md.
- `models/` — TS types mirroring docs/architecture/data-model.md.
